import { Options } from '../../options';
import { StageResult } from '../../index';
export default class ResugarStage {
    static run(content: string, options: Options): StageResult;
    private static getPluginsForOptions;
}
