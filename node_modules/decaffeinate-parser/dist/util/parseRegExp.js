"use strict";
exports.__esModule = true;
var parser_1 = require("@codemod/parser");
/**
 * Parses JavaScript source representing a regular expression.
 */
function parseRegExp(string) {
    var expressionStatement = parser_1.parse("(" + string + ")").program
        .body[0];
    var literal = expressionStatement.expression;
    return literal;
}
exports["default"] = parseRegExp;
