"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const __1 = require("..");
const incrementNumbersPlugin = {
    visitor: {
        NumericLiteral(path) {
            path.node.value += 1;
        },
    },
};
test('prints with recast by default', () => {
    expect(__1.transform('var a=1;').code).toBe('var a=1;');
});
test('allows printing with babel', () => {
    expect(__1.transform('var a=1;', { printer: __1.Printer.Babel }).code).toBe('var a = 1;');
});
test('allows printing with prettier', () => {
    expect(__1.transform('var a=1;', { filename: 'index.js', printer: __1.Printer.Prettier })
        .code).toBe('var a = 1\n');
});
test('transforms using a custom babel plugin', () => {
    expect(__1.transform('var a=1', {
        plugins: [incrementNumbersPlugin],
    }).code).toBe('var a=2');
});
test('parses with as many parser plugins as possible', () => {
    expect(() => __1.transform('a ?? b').code).not.toThrow();
});
//# sourceMappingURL=test.js.map