import { ParserOptions } from '@codemod/parser';
import { File } from '@babel/types';
import { PluginObj } from './BabelPluginTypes';
export declare function parse(code: string, options: ParserOptions, parse: (code: string, options: ParserOptions) => File): File;
export declare function generate(ast: File): {
    code: string;
    map?: object;
};
export default function (): PluginObj;
