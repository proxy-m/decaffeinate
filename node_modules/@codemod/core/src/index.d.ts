import { TransformOptions as BabelTransformOptions, BabelFileResult } from '@babel/core';
export declare enum Printer {
    Recast = "recast",
    Prettier = "prettier",
    Babel = "babel"
}
export interface TransformOptions extends BabelTransformOptions {
    printer?: Printer;
}
/**
 * Transform `code` using `@babel/core` with a custom parser & printer, which
 * defaults to `recast`. Additionally, `@codemod/parser` is used to enable as
 * many parser plugins as possible.
 */
export declare function transform(code: string, { printer, ...options }?: TransformOptions): BabelFileResult;
